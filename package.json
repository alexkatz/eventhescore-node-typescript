{
  "name": "eventhescore-node",
  "version": "1.0.0",
  "description": "A simple app for keeping scores of games!",
  "main": "src/server.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "start-client": "cd client && yarn start",
    "clear": "rm -r dist",
    "lint-fix": "tslint -c tslint.json -p tsconfig.json --type-check --fix",
    "lint": "tslint -c tslint.json -p tsconfig.json --type-check --format codeFrame --force",
    "watch": "tsc-watch --project tsconfig.json --onSuccess \"yarn lint\""
  },
  "devDependencies": {
    "@types/boom": "^4.3.5",
    "@types/hapi": "^16.1.7",
    "@types/inert": "^4.2.2",
    "@types/pg": "^6.1.43",
    "@types/redux": "^3.6.0",
    "tsc": "^1.20150623.0",
    "tsc-watch": "1.0.7",
    "tslint": "^5.5.0",
    "typescript": "^2.4.2"
  },
  "dependencies": {
    "boom": "^5.2.0",
    "hapi": "^16.5.0",
    "hapi-require-https": "^2.1.0",
    "inert": "^4.2.1",
    "pg": "^7.0.2"
  }
}
