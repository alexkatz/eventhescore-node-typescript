{
  "name": "eventhescore-node",
  "version": "1.0.0",
  "description": "A simple app for keeping scores of games!",
  "main": "src/server.ts",
  "license": "MIT",
  "scripts": {
    "build": "webpack",
    "publish": "webpack --production",
    "clean": "rm -r dist",
    "lint-fix": "tslint -c tslint.json -p tsconfig.json --type-check  --fix",
    "lint": "tslint -c tslint.json -p tsconfig.json --type-check --format stylish"
  },
  "devDependencies": {
    "@types/hapi": "^16.1.7",
    "awesome-typescript-loader": "^3.2.2",
    "babel-core": "^6.25.0",
    "tslint": "^5.5.0",
    "tslint-loader": "^3.5.3",
    "typescript": "^2.4.2",
    "webpack": "^3.4.1"
  },
  "dependencies": {
    "hapi": "^16.5.0"
  }
}
